type AuthRequest = {
  username: string;
  password: string;
};

type UserIdParam = {
  targetUserId: string;
};

type UserUserNameParam = {
  targetUserUserName: string;
};

type LinkParam = {
  link: string;
};
